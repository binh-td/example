import React from 'react'

export default function Home() {
  return (
    <div>Home 43242424</div>
  )
}

// git clone + "link copy tu github" để tải repo về local (máy mình)
// sử dụng git checkout để chuyển branch có sẵn trên remote hoặc local repo (nhánh trên git). VD: git checkout "feature/products"
// git checkout -b + tên nhánh để chuyển sang branch mới tạo. VD: git checkout -b "feature/products"
// git add + "tên file" hoặc git add . để đẩy toàn bộ file thay đổi lên staging area. VD: git add .
// git commit -m "" để commit code từ staging area lên local repo. VD: git commit -m "update"
// git push để đẩy code từ local repo lên remote repo
// git pull để lấy code từ remote repo xuống local repo